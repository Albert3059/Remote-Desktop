# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables.
# This folder is OneDrive-synced, so secrets must not live in a file here at
# all — use scripts\set-secrets.ps1. .env.local.example is the template and is
# deliberately not ignored.
.env
.env.*
!.env.local.example

# Common ignores
node_modules
.next/
.DS_Store