{
  "name": "clouddesk-windows-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc --outDir dist",
    "package:check": "node -e \"const p=require('./packaging.json'); if(!p.outboundOnly || !p.signing.required) process.exit(1); console.log('Packaging metadata valid')\""
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.7.3"
  }
}
